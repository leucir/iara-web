The embeddings are structured based on the OFFERING_NAME and the DOMAIN_TYPE.

/EMBEDDING_INDEX_FOLDER/OFFERING_NAME/DOMAIN_TYPE/<name of the embeddings>

The <name of the embeddings> value is created based on the file name where the original text came from. It should represent an entity associated with the OFFERING_NAME and DOMAIN_TYPE.